To define loaded set appsetting.json VendorSettings.LoaderType to  FileLoader or SqlServerLoader.
Start Vendor.API first (at `http://localhost:5138`)
swagger UI will open.

run UI:
npm start
app opens at `http://localhost:4200`.

Backend code:
Vendor.API - start it opens swagger
Vendor.Logic

UI:
Vendor.UI - generated.

License:
Distribution prohibited.
