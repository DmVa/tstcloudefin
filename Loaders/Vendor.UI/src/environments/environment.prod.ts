export const environment = {
  production: true,
  // Update with the deployed Vendor.API origin, or leave empty if the app
  // is served from the same origin/reverse proxy as the API.
  apiUrl: ''
};
